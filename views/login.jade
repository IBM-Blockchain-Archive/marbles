extends ./template/layout.jade
	
	
// --------------- Header -------------- //
block custom_header
	script(type='text/javascript').
		$(document).ready(function(){

			/*
			//roll_ball();
			function roll_ball() {
				$('#proposeMarbleStable').removeClass('hideBorders');
				$('#marbleBorderTop').hide();
				$('#marbleBorderBottom').hide();
				$('#marbleBorderLeft').hide();
				$('#marbleBorderRight').hide();
				var move = 200;
				$('#proposeMarbleStable').animate({left: '+='+move}, {
					duration: 2000,
					step: function(distance) {
						var degree = distance * 360 / move;
						$('#proposeMarbleStable').css('transform','rotate(' + degree + 'deg)');
					},
					complete: shrink_ball
				});
			}

			var dist = 50;
			$('#marbleBorderTop').animate({top: '+=' + dist}, {duration: 2000});
			$('#marbleBorderBottom').animate({top: '-=' + dist}, {duration: 2000});
			$('#marbleBorderLeft').animate({left: '+=' + dist}, {duration: 2000});
			$('#marbleBorderRight').animate({left: '-=' + dist}, {duration: 2000});
			

			var move = 200;
			$('#dummy').animate({left: '+=' + move}, {
				duration: 2000,
				step: function(distance) {
					var degree = distance * 360 / move;
					$('#ballWrap').css('transform','rotate(' + degree + 'deg)');
				},
				complete: roll_ball
			});

			function shrink_ball(){
				$('#proposeMarbleStable').animate({width: 0, height:0}, {
					duration: 300,
					complete: function(){
						$('#proposeMarbleStable').hide();
						setTimeout(function(){grow_ball();},1000);
					}
				});
			}

			function grow_ball(){
				$('#proposeMarbleStable').show();
				$('#proposeMarbleStable').animate({width: 40, height:40}, {
					duration: 600,
					complete: function(){
						//$('#proposeMarbleStable').hide();
					}
				});
			}*/
		});
		
		
// --------------- Navigation Bar  -------------- //
block navpanel
	include ./template/nav.jade
	
	
// --------------- Main Stuff  -------------- //
block content
	#notificationWrap
	
	#contentPanel
		
		// --------------- Welcome Panel
		#welcomePanel
			h2(style="font-weight: 600;") WELCOME TO THE MARBLES DEMO APP

			#adminText
				span Hello again 
				span#loginCompany #{bag.marble_company} 
				span admin !

			p 
				span This application allows you to simulate a simple asset transaction over a Hyperledger Fabric blockchain. 
				span Connect multiple peers to trade with a larger marble userbase. 
				span In this application, you can: 
			
			#split1x3Wrap
				.split1x3
					.welcomeTitle
						span Trade
						//br
							span Assets
					br
					.blub1x3 Transfer assets/marbles across the marbles user base
					img(src="/imgs/welcome_img1.png").welcomeImg
				.split1x3
					.welcomeTitle
						span Create
						//br
							span Marbles
					br
					.blub1x3 Create new assets/marbles
					img(src="/imgs/welcome_img2.png").welcomeImg
				.split1x3
					.welcomeTitle
						span Verify
						//br
							span Ownership
					br
					.blub1x3 Verify marble transactions
					img(src="/imgs/welcome_img3.png").welcomeImg
			br
			br

			p 
				span For more information, please visit our 
				a(href="https://github.com/IBM-Blockchain/marbles/tree/v3.0") GitHub
				span.


		// --------------- Login Panel
		form#loginPanel(method="POST" action="#")
			p Enter
			hr
			input(type="text" placeholder="username" value="admin" name="username")
			button(type="submit") Login


		//#test(style="position: relative;")
			#ballWrap
				div.ball#proposeMarbleStable.hideBorders
				div.ball#marbleBorderTop
				div.ball#marbleBorderBottom
				div.ball#marbleBorderLeft
				div.ball#marbleBorderRight
			#dummy
		
block footer
	//include ./template/blockchain.jade